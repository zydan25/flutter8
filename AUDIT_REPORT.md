# تقرير فحص مشروع «التخفيض الصح»

تاريخ الفحص: 2026-09-12

## 1. الخلاصة التنفيذية

المشروع الموجود داخل الأرشيف ليس مشروع Flutter، بل تطبيق Web مبني باستخدام React 19 + Vite 6، مع Firebase وFirestore. الأرشيف يحتوي على نسخة build جاهزة وملفات إعداد/patch، لكنه لا يحتوي على مجلد `src/` أو ملفات المصدر الأساسية اللازمة لإعادة build التطبيق من الصفر.

الحالة: **التطبيق المبني قابل للتحميل من حيث البنية، لكن المشروع المصدر غير مكتمل لإعادة البناء والتطوير بشكل سليم.**

## 2. ما تم اختباره

- فك الأرشيف وفحص جميع الملفات والمسارات.
- فحص `package.json` والإعدادات.
- فحص `index.html` وروابط CSS/JS والأيقونات والـ manifest.
- فحص JavaScript الرئيسي باختبار syntax بواسطة Node.js: **نجح**.
- التحقق من أن الأصول التي يطلبها `index.html` موجودة داخل `public/`: **نجح**.
- محاولة تثبيت الاعتماديات وتشغيل build: **لم يكتمل**؛ `npm ci` مرفوض لعدم وجود `package-lock.json`، وبعد محاولة `npm install` لم تتوفر البيئة خلال المهلة، وبعد ذلك كان `vite` غير موجود؛ لذلك لم أعتبر build ناجحًا.
- فحص قواعد Firestore: **وجدت مشكلة أمنية حرجة**.
- فحص النسخة المبنية للوظائف الظاهرة: توجد مؤشرات واضحة على تسجيل الدخول، الطلبات، السلة، المفضلة، الإدارة، العملاء، المحادثات، الشحن، العروض/الخصومات، المقاسات، الألوان، المحافظات، المخزون، البحث ورفع الصور.

## 3. الموجود ويبدو أنه يعمل في النسخة المبنية

- واجهة عربية RTL وشاشة Splash.
- PWA manifest وأيقونات وService Worker.
- تسجيل الدخول/حالة المصادقة.
- إدارة المنتجات والعروض والخصومات.
- السلة والمفضلة والطلبات.
- لوحة/وظائف إدارية.
- إدارة العملاء والمستخدمين.
- المحادثات والرسائل.
- الشحن والمحافظات.
- المقاسات والألوان والمخزون.
- رفع الصور والبحث.
- مراجعات المنتجات وتخزينها محليًا عبر `localStorage`.

هذه النقاط مستنتجة من النسخة المبنية، وليست إثباتًا لاختبار كل شاشة تفاعليًا في متصفح حقيقي.

## 4. ما لا يعمل أو لا يمكن إثباته حاليًا

- إعادة البناء من المصدر غير ممكنة بشكل موثوق لأن `src/` غير موجود.
- `npm ci` غير صالح مباشرة لغياب `package-lock.json`.
- لم أتمكن من إثبات نجاح `vite build` من المصدر.
- لم يتم إجراء اختبار حي كامل لخدمات Firebase/Authentication/Firestore/Storage.

## 5. مشكلة أمنية حرجة في Firestore

`firestore.rules` يحتوي على مسارات حساسة تسمح بالقراءة والكتابة باستخدام `if true`، ومنها users وdeleted_users وadmin_roles وorders وchats/messages.

إذا كانت هذه القواعد هي المنشورة فعليًا، فبيانات العملاء والطلبات والمحادثات والصلاحيات معرضة للوصول أو التعديل غير المصرح به.

وجود `isAdmin()` لا يكفي طالما أن المسارات لا تستخدمها في شروط السماح.

## 6. Firebase والإعدادات

يوجد إعداد Firebase للواجهة. بيانات تعريف تطبيق الويب ليست بديلًا عن قواعد حماية جيدة؛ الحماية الفعلية يجب أن تكون في Authentication وFirestore/Storage Rules والـ backend عند الحاجة.

توجد أيضًا عناوين بريد ثابتة داخل منطق الإدارة في القواعد. الأفضل استخدام Custom Claims أو آلية أدوار موثوقة.

## 7. مشكلات بنيوية

- اسم المستودع `flutter8` بينما المحتوى React/Vite وليس Flutter.
- غياب المصدر يجعل التطوير والصيانة وإعادة البناء صعبة.
- وجود `dist_backup` وملفات `.bak` وworking_bak يضيف نسخًا قديمة إلى المشروع.
- ملفات patch موجودة دون المصدر الأصلي الذي تعمل عليه.
- أمر `lint` الحالي ليس lint فعليًا؛ هو مجرد `echo 'No lint errors'`.

## 8. ما يحتاجه المشروع

### أولوية حرجة
1. استعادة ملفات `src/` والمصدر الأصلي.
2. إصلاح Firestore Rules وإلغاء `allow ...: if true` من البيانات الحساسة.
3. ضبط صلاحيات الإدارة بطريقة موثوقة.
4. إضافة اختبارات فعلية للمصادقة والطلبات والبيانات.

### أولوية عالية
1. إضافة `package-lock.json` أو توحيد الاستخدام على Bun.
2. إضافة lint وtypecheck حقيقيين.
3. فصل build والنسخ الاحتياطية عن المصدر.
4. توثيق Firebase وStorage Rules ومخطط البيانات.
5. إعداد CI للبناء والفحص.

## 9. الحكم النهائي

**النسخة الموجودة أقرب إلى نسخة تشغيل مبنية منها إلى مشروع تطوير مكتمل.** الوظائف الرئيسية ظاهرة داخل bundle، لكن المصدر مفقود، والبناء من الصفر غير مثبت، وقواعد Firestore غير آمنة للإنتاج.

التوصية: استعادة المصدر أولًا، ثم إصلاح الصلاحيات والقواعد، ثم إنشاء build نظيف واختبارات تشغيل حقيقية قبل اعتماد المستودع كنسخة إنتاج.
